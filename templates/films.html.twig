<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Film Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://kit.fontawesome.com/28eab93eea.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/assets/css/films.css">
</head>
<body>
<div class="container mt-5">
    <div class="film-content">
        <img src="/assets/images/film1.jpg" class="film-img" alt="Film Name">
        <div class="film-overview">
            <h1>Film Title</h1>
            <p>Amazing film overview goes here.</p>
            <p><strong>Type:</strong> Action</p>
            <a href="/" class="btn btn-primary">Back to Home</a>
        </div>
    </div>

    <div class="reservation-container text-center">
        <h3>Seat Reservation</h3>
        <div class="screen mx-auto mb-3">Screen</div>
        <form action="#" method="POST">
            <div class="seat-layout d-flex flex-column align-items-center">
                {% for row in 1..5 %}
                    <div class="d-flex justify-content-center mb-2">
                        {% for col in 1..6 %}
                            {% set seat_number = (row - 1) * 6 + col %}
                            <div class="seat me-2">
                                <label>
                                    <input type="checkbox" name="seats[]" value="{{ seat_number }}">
                                    Seat {{ seat_number }}
                                </label>
                            </div>
                        {% endfor %}
                    </div>
                {% endfor %}
            </div>
            <button type="submit" class="btn btn-primary mt-3 mx-auto">Reserve Seats</button>
        </form>
    </div>



    <div class="comments-section mt-5">
        <h4>Comments</h4>
        <p><strong>John Doe:</strong> Amazing movie!</p>
        <p><strong>Jane Smith:</strong> Loved the action scenes!</p>
        <form class="mt-3">
            <textarea class="form-control" placeholder="Add your comment..."></textarea>
            <button type="submit" class="btn btn-primary mt-2">Add Comment</button>
        </form>
    </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
